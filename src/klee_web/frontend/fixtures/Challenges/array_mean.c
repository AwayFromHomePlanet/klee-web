//----- ARRAY MEAN -----\\
// Write a function that takes in an array of floats and the length of the array
// and returns the mean of the array.
// e.g. array_mean([1.0, 2.0, 3.0, 3.0, 2.0], 5) = 2.2

float array_mean(float *array, int length) {
  return 0;
}
